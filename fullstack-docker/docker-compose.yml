version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: mongodb
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password123
    volumes:
      - mongo-data:/data/db
    networks:
      - backend

  api:
    build: ./api
    container_name: api
    environment:
      MONGODB_URI: mongodb://admin:password123@mongodb:27017/testdb
      PORT: 3000
    depends_on:
      - mongodb
    networks:
      - backend
      - frontend

  nginx:
    image: nginx:alpine
    container_name: nginx
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api
    networks:
      - frontend

volumes:
  mongo-data:

networks:
  backend:
  frontend:
